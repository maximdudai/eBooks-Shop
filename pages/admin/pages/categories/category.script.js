'use strict'

let VISIBLE_TYPE_CATEGORY = 1;
const navLinkVisible = document.querySelectorAll(".menuOption");


navLinkVisible.forEach((menuOptionItem) => {
    menuOptionItem.addEventListener('click', () => {

        // toggle info visible by choosing a <li> item

    });
});